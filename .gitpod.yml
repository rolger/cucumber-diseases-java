image:
  file: .gitpod.Dockerfile

tasks:
  - init: mvn install -DskipTests=false

vscode:
  extensions:
    - CucumberOpen.cucumber-official

jetbrains:
    intellij:
        plugins:
            - gherkin
            - cucumber-java
